# POKERTOOL-HEADER-START
# ---
# schema: pokerheader.v1
# project: pokertool
# file: requirements.txt
# version: v28.0.0
# last_commit: '2025-09-23T08:41:38+01:00'
# fixes:
# - date: '2025-09-25'
#   summary: Enhanced enterprise documentation and comprehensive unit tests added
# ---
# POKERTOOL-HEADER-END
# Poker Assistant - Python Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
# NOTE: NumPy 2.x is required for opencv-python 4.12+ compatibility
# pytesseract and pandas are compatible with numpy 2.x
numpy>=2.0.0,<2.3.0
Pillow>=10.0.0

# Machine Learning dependencies
scikit-learn>=1.3.0
scikit-image>=0.25.0,<0.26.0
pandas>=2.0.0
joblib>=1.3.0
# tensorflow==2.16.2  # Temporarily disabled due to Python 3.13 compatibility
# paddlepaddle>=3.0.0  # Temporarily disabled due to Python 3.13 compatibility
# torch>=2.0.0,<3.0.0  # Temporarily disabled due to Python 3.13 compatibility
# easyocr>=1.7.0  # Temporarily disabled due to Python 3.13 compatibility (requires torch)

# Screen scraping and computer vision
# NOTE: opencv-python-headless is more compatible with macOS (no GUI dependencies)
# opencv-python 4.12+ requires numpy>=2.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
mss>=9.0.0
requests>=2.32.0
websocket-client>=1.6.0
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'
pyvirtualdisplay>=3.0.0; sys_platform == 'linux'

# Testing and development
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0
mutmut>=2.4.0
psutil>=5.9.0
packaging>=24.0

# Performance monitoring
coverage>=7.0.0

# Platform-specific (uncomment as needed)
# pywin32>=306  # Windows only
# python-xlib>=0.33  # Linux only

autopep8>=2.0.0
black>=23.0.0
isort>=5.12.0
pylint>=2.17.0
autoflake>=2.0.0
mypy>=1.0.0

# API and Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0

# Distributed tracing (OpenTelemetry)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-exporter-otlp>=1.20.0  # Optional: for exporting to OTLP collectors
locust>=2.42.0

# LangChain and AI/ML Memory
langchain>=0.3.0
langchain-community>=0.3.0
chromadb>=0.5.0
# sentence-transformers>=3.0.0  # Disabled: requires torch (Python 3.13 compatibility issue)
# Note: ChromaDB will use default embedding function (sentence-transformers backend via onnx runtime)
tiktoken>=0.8.0  # Token counting for LLM context management
pydantic>=2.0.0  # Already used by FastAPI, but ensuring compatibility
onnxruntime>=1.16.0  # Lightweight runtime for ChromaDB embeddings (no torch needed)
